@inherits HtmlElement

@code {
    [CascadingParameter]
    private AeTab TabControl { get; set; }
    [Parameter]
    public string Key { get; set; }
    [Parameter]
    public bool Disable { get; set; }
    [Parameter]
    public bool Deletable { get; set; }
    [Parameter]
    public RenderFragment Header { get; set; }
    [Parameter]
    public RenderFragment Body { get; set; }

    public string _Classname => $"ae tabpage {_inputClass}";

    protected override void OnInitialized()
    {
        if (string.IsNullOrWhiteSpace(Key)) throw new ArgumentException("Tab key is not valid");
        TabControl.AddTab(this);
    }

    public void RemoveTab()
    {
        TabControl.RemoveTab(this);
    }
}
