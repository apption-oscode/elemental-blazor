@inherits HtmlElement

<div class="@_classname" style="@Style" id="@Id">
    @ChildContent
</div>

@code {
    [Parameter]
    public bool Vertical { get; set; }
    private string _classname => $"ae flex {Classname} {(Vertical ? "vertical" : "")}";
}
